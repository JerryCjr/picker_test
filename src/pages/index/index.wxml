<view class="h5">
  <component-picker
    bind:resolve="resolve"
    initial="{{initObj}}"
  >
    <!-- 这部分内容将被放置在组件 <slot> 的位置上 -->
    <view
      class="test"
      slot="picker"
    >{{areaShow}}</view>
  </component-picker>
</view>
